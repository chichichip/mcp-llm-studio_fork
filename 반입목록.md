# 폐쇄망 반입 목록

개발 PC → GitHub → 회사 PC(clone) → 폐쇄망. **사내 PC 파이썬 3.10 기준.**

반입물은 세 종류이고 조달 경로가 다르다:

| 종류 | 조달 | 예 |
|---|---|---|
| **코드** | `git pull` | 이 저장소 전체 |
| **파이썬 패키지** | `wheelhouse/`에 이미 들어 있음 (git으로 따라옴) | PyMuPDF, fastmcp |
| **실행 파일·모델** | USB 등 별도 매체 (100MB 넘어 git에 못 넣음) | `.gguf`, `llama-server.exe` |

## 파이썬 패키지는 이미 저장소에 들어 있다 (`wheelhouse/`)

`cp310` + `win_amd64` 휠 100개를 `wheelhouse/`에 넣어 뒀다. **따로 받을 것 없이
`git pull` 하고 설치하면 된다.**

```cmd
install_requirements.bat
```

미러 주소를 비워 두면 `--no-index`로 이 폴더에서만 설치한다. 갱신하는 법과
포함 목록은 `wheelhouse/README.md` 참고.

아래 표는 **각 패키지가 없을 때 무엇이 안 되는지**를 적어 둔 것이다 — 설치가
일부 실패했을 때 무엇을 포기하고 무엇을 고쳐야 하는지 판단하는 용도다.

---

## 1. 파이썬 패키지

### 필수 — 없으면 해당 기능이 통째로 안 됨

| 패키지 | 쓰는 곳 | 없으면 |
|---|---|---|
| `pywin32` | Office/Outlook COM 전부 | 서버는 뜨지만 모든 도구가 안내만 반환 |
| `fastmcp>=3.0` | `mcp_server/*.py` 전부 | **import 단계에서 죽는다.** 2.x는 인자 이름이 달라 안 됨 (개발 PC 검증 3.4.4) |
| `fastapi>=0.95`, `uvicorn>=0.29` | llm_studio 앱 | 앱이 안 뜸 |
| `openai>=1.30`, `mcp>=1.0` | llm_studio (LLM 호출·MCP 클라이언트) | 앱이 안 뜸 |

### Vision RAG — 지침서·스펙을 그림으로 읽는 경로

| 패키지 | 상태 | 없으면 |
|---|---|---|
| `PyMuPDF` | ✅ wheelhouse | PDF 렌더링 불가 → vision 경로 전체가 막힘 |
| `Pillow` | ✅ wheelhouse | 이미지 축소 불가 → `--dpi`로만 조절(413 위험) |
| `qdrant-client` | ✅ wheelhouse | sqlite BLOB 벡터로 저하(동작은 함, 느림) |

### 표준품 선정 (spec-reader)

| 패키지 | 상태 | 없으면 |
|---|---|---|
| `requests` | ✅ wheelhouse | VLM 판독(`read_spec.py`) 불가 |
| `openpyxl` | ✅ wheelhouse | `catalog.py`가 엑셀을 못 읽음 (CSV로 저장하면 우회 가능) |

### 선택 — 없어도 우아하게 저하

| 패키지 | 없으면 |
|---|---|
| `pypdf>=4.0` | 평문 PDF의 direct 백엔드 비활성 (DRM PDF는 Word COM으로 계속 읽힘) |
| `python-docx>=1.1` | llm_studio 채팅 첨부에서 .docx 텍스트 추출만 비활성 |
| `numpy` | sqlite 벡터 코사인 계산이 느려짐 (qdrant-client 깔면 따라옴) |
| `ansys-mapdl-core` | ANSYS 도구 전부 안내만 반환 (미러 등록 여부 미확인) |

---

## 2. 실행 파일·모델 (USB 등 별도 매체)

| 반입물 | 용도 | 없으면 |
|---|---|---|
| `llama-server.exe` + CUDA DLL | 로컬 LLM 서빙 | 사내 게이트웨이만 쓸 거면 불필요 |
| 대화용 `.gguf` (Gemma 12B IT QAT 등) | 로컬 LLM | 위와 같음 |
| **임베딩 `.gguf`** | RAG 의미 검색 | **검색이 키워드 전용으로 저하** — 아래 참고 |

### 임베딩 서버가 뭔가

RAG 검색은 두 가지를 합쳐서 한다:

- **키워드 검색** — 글자가 겹치면 찾는다. 별도 서버 불필요(sqlite FTS5 내장).
- **벡터(의미) 검색** — "체결두께"로 물어도 "그립"이 적힌 문단을 찾는다.
  문장을 숫자 배열로 바꾸는 **임베딩 모델**이 필요하고, 그 모델은 `llama-server`를
  하나 더 띄워서 서빙한다. 대화용 LLM과는 **다른 모델, 다른 포트**다.

```cmd
llama-server -m C:\models\embeddinggemma.gguf --embeddings -ngl 0 --port 8001
```

`-ngl 0` = CPU 상주. 임베딩 모델은 작아서 GPU를 안 잡아먹는 게 낫다.
`rag_core.py`가 기본으로 `http://127.0.0.1:8001/v1`을 본다(`RAG_EMBED_URL`로 변경).

**확인해 볼 것: 사내 게이트웨이에 이미 임베딩 엔드포인트가 있을 수 있다.**
VLM을 호스팅하는 곳이 `/v1/embeddings`도 열어 뒀다면 모델 파일을 반입할 필요가 없다.

```cmd
curl http://<사내게이트웨이>/v1/models
```

목록에 embedding 계열(`bge`, `e5`, `embeddinggemma` 등)이 보이면
`--embed-url http://<사내게이트웨이>/v1`로 지정만 하면 된다.

**없어도 진행은 된다.** 키워드 인덱스만 만들고, 나중에 임베딩 서버를 켠 뒤
`--reindex`로 다시 돌리면 벡터가 붙는다.

---

## 3. 사내 주소 (반입물 아님, 설정값)

| 항목 | 어디에 |
|---|---|
| VLM(Gemma) 주소·모델명 | `--vlm-url` / `--vlm-model`, 또는 `RAG_VLM_URL` / `RAG_VLM_MODEL` |
| 임베딩 서버 주소 | `--embed-url`, 또는 `RAG_EMBED_URL` |
| spec-reader용 같은 주소 | `spec-reader/config.py` (`config.example.py` 복사해 채움, `.gitignore`에 있음) |

---

## 4. 반입 후 첫 확인 순서

```cmd
:: 패키지가 다 들어갔는지
python -c "import fitz, PIL, fastmcp, qdrant_client; print('OK')"

:: VLM이 실제로 페이지를 읽는지 (한 장만 — 전체 인덱싱 전에 반드시)
python mcp_server\vision_ingest.py --probe "C:\지침서\지침서.pdf" --vlm-url http://<사내VLM>/v1

:: 임베딩 서버가 붙는지
python mcp_server\rag_indexer.py --status
```

`--probe`가 413을 뱉으면 `--dpi 120` 처럼 낮춘다. 100쪽을 다 돌린 뒤 실패를 발견하면
시간이 아깝다 — 한 장으로 먼저 확인할 것.
